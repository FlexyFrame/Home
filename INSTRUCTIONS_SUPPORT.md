# 📋 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ СИСТЕМЫ ПОДДЕРЖКИ FLEXYFRAME

## 🎯 Что было настроено

### 1. Система тикетов поддержки
- ✅ База данных для тикетов и сообщений
- ✅ Автоматическое создание тикета при оплате заказа
- ✅ Изоляция тикетов (пользователи видят только свои)
- ✅ Админ-панель для ТП

### 2. Два бота
- **Основной бот** (`bot.js`) - обработка заказов, оплаты
- **Бот поддержки** (`support_bot.js`) - работа с тикетами

## 🚀 БЫСТРЫЙ СТАРТ

### Шаг 1: Настройка токенов

В файле `.env` добавьте:

```env
# Основной бот (уже есть)
TELEGRAM_BOT_TOKEN=8059103322:AAHf8ql_pq1FsQEhL3Xd6Dodku4DkCMIu2Y

# Админ-бот (уже есть)
ADMIN_BOT_TOKEN=8294078069:AAFN3ehh4nY4biPenrti4bWZwpOJFTDq6Gs

# Бот поддержки (НОВОЕ!)
SUPPORT_BOT_TOKEN=8294078069:AAFN3ehh4nY4biPenrti4bWZwpOJFTDq6Gs

# Ваш ID администратора (уже есть)
ADMIN_CHAT_ID=1131158010
```

**ВАЖНО:** Для бота поддержки нужен отдельный токен! Создайте нового бота через @BotFather и вставьте его токен.

### 🆕 ИЗМЕНЕНИЯ В ОПЛАТЕ:

**В ТГ боте:**
- ❌ Убрана кнопка "💳 Оплатить онлайн" (пока не работает)
- ✅ Оставлена только кнопка "✅ Оплатил(а)"
- ✅ В инструкции указана оплата через **ЮMoney**
- ✅ В комментарии к оплате нужно указывать: `Заказ #ID`

**Процесс оплаты:**
1. Пользователь оплачивает по реквизитам ЮMoney
2. Указывает в комментарии: `Заказ #1` (например)
3. Нажимает "✅ Оплатил(а)"
4. Система подтверждает оплату и создает тикет

### Шаг 2: Запуск ботов

**Вариант 1: Оба бота одновременно**
```bash
npm run both
```

**Вариант 2: По отдельности**
```bash
# Основной бот
npm run start

# Бот поддержки (в другом терминале)
npm run support
```

## 📖 КАК РАБОТАЕТ СИСТЕМА

### Для ПОЛЬЗОВАТЕЛЯ:

1. **Оплата заказа**
   - Пользователь оплачивает заказ в основном боте
   - ✅ Автоматически создается тикет поддержки
   - Пользователь получает уведомление о создании тикета

2. **Общение с поддержкой**
   - Пользователь переходит в @FlexyFrameSupportBot
   - Отправляет `/start` → видит список своих тикетов
   - Чтобы написать сообщение: `#1 Привет, у меня вопрос...`
   - Получает ответы от ТП в этом же чате

### Для ТЕХПОДДЕРЖКИ:

1. **Админ-панель**
   - Откройте @FlexyFrameSupportBot
   - Отправьте `/start` → увидите список активных тикетов

2. **Ответ пользователю**
   - Формат: `#1 Ваш ответ`
   - Пример: `#1 Мы исправим это в течение 24 часов`

3. **Закрытие тикета**
   - Формат: `close 1`
   - Пользователь получит уведомление о закрытии

4. **Просмотр истории**
   - Формат: `/history 1`

## 🔒 БЕЗОПАСНОСТЬ И ИЗОЛЯЦИЯ

### Что защищено:
- ✅ Пользователи видят только свои тикеты
- ✅ Тикет уникален по order_id
- ✅ История переписки сохраняется в БД
- ✅ Админ видит все тикеты

### Как работает изоляция:
```javascript
// Пользователь видит только свои тикеты
SELECT * FROM tickets WHERE user_id = ?

// Админ видит все открытые тикеты
SELECT * FROM tickets WHERE status = 'open'
```

## 📊 СТРУКТУРА БАЗЫ ДАННЫХ

### Таблица tickets
```sql
id INTEGER PRIMARY KEY
user_id INTEGER          -- ID пользователя
order_id INTEGER         -- ID заказа
status TEXT              -- open/closed
chat_id INTEGER          -- ID чата
created_at DATETIME      -- Дата создания
closed_at DATETIME       -- Дата закрытия
```

### Таблица ticket_messages
```sql
id INTEGER PRIMARY KEY
ticket_id INTEGER        -- ID тикета
from_user INTEGER        -- 1=пользователь, 0=админ
message TEXT             -- Текст сообщения
timestamp DATETIME       -- Время отправки
```

## 🎯 ПРИМЕРЫ СЦЕНАРИЕВ

### Сценарий 1: Новый заказ
1. Пользователь оплачивает → создается тикет #1
2. Пользователь видит: "🎫 Создан тикет #1"
3. Админ видит в панели: "Тикет #1 от пользователя ID: 1131158010"

### Сценарий 2: Вопрос по заказу
1. Пользователь пишет в поддержку: `#1 Какой статус заказа?`
2. Админ получает уведомление
3. Админ отвечает: `#1 Заказ в печати, завтра будет готов`
4. Пользователь получает ответ

### Сценарий 3: Закрытие тикета
1. Админ пишет: `close 1`
2. Тикет закрывается
3. Пользователь получает: "✅ Тикет #1 закрыт"

## 🛠️ ОТЛАДКА

### Проверить работу:
```bash
# Запустите ботов
npm run both

# В основном боте: создайте заказ и оплатите
# В поддержке: проверьте список тикетов /start
```

### Проверить базу данных:
```bash
sqlite3 flexyframe.db
SELECT * FROM tickets;
SELECT * FROM ticket_messages;
```

## 📝 ВАЖНЫЕ ЗАМЕТКИ

1. **Токены**: Используйте разные токены для разных ботов
2. **Админ ID**: Убедитесь, что ADMIN_CHAT_ID правильный
3. **База данных**: Создается автоматически при первом запуске
4. **Уведомления**: Работают только если настроен админ-бот

## 🚨 ЧАСТЫЕ ПРОБЛЕМЫ

**Проблема**: "Тикет не создается после оплаты"
- **Решение**: Проверьте, что SUPPORT_BOT_TOKEN добавлен в .env

**Проблема**: "Пользователь не видит тикеты"
- **Решение**: Убедитесь, что он пишет в правильного бота (@FlexyFrameSupportBot)

**Проблема**: "Админ не получает уведомления"
- **Решение**: Проверьте ADMIN_CHAT_ID и что админ-бот запущен

## 📞 КОНТАКТЫ

- Основной бот: @flexyframe_bot
- Бот поддержки: @FlexyFrameSupportBot (после создания)
- Email: designstudioflexyframe@gmail.com

---

**Успехов в настройке! 🎨**