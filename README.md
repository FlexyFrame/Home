# FlexyFrame - Telegram Bot + PWA

Полнофункциональная платформа для продажи уникальных граффити-артов через Telegram бота с интеграцией YooKassa и PWA веб-сайтом.

## 🚀 Быстрый старт

### 1. Настройка GitHub Secrets

Откройте репозиторий на GitHub и перейдите в:
**Settings → Secrets and variables → Actions → New repository secret**

Добавьте следующие секреты:

| Secret | Описание | Пример |
|--------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | `8059103322:AAHf8ql_pq1FsQEhL3Xd6Dodku4DkCMIu2Y` |
| `ADMIN_CHAT_ID` | ID администратора | `1131158010` |
| `YOOKASSA_SHOP_ID` | ID магазина YooKassa | `1228208` |
| `YOOKASSA_SECRET_KEY` | Секретный ключ YooKassa | `live_lj3HcJaRMlnDbvA9903y3tcvdpuyrdEqK_8dt0SLgrU` |
| `SITE_URL` | URL вашего сайта | `https://yourusername.github.io/flexyframe` |

### 2. Настройка GitHub Pages

1. Перейдите в **Settings → Pages**
2. **Source**: Deploy from branch
3. **Branch**: main
4. **Folder**: / (root)
5. Нажмите **Save**

### 3. Запуск деплоя

```bash
# Коммит всех изменений
git add .
git commit -m "Initial deployment with GitHub Actions"

# Push в репозиторий
git push origin main
```

### 4. Проверка

1. Откройте **Actions** на GitHub
2. Дождитесь завершения workflow "Deploy FlexyFrame Bot"
3. Откройте сайт: `https://yourusername.github.io/flexyframe`
4. Проверьте бота: `@flexyframe_bot`

## 📁 Структура проекта

```
FlexyFrame/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions workflow
├── Аркейн/                     # Картины
├── Давид/                      # Картины
├── Железный Человек/           # Картины
├── Земфира/                    # Картины
├── ЛОГОТИП/                    # Логотип
├── Магическая битва/           # Картины
├── Скрудж/                     # Картины
├── Танос/                      # Картины
├── Live/                       # Картины
├── bot.js                      # Основной бот + сервер
├── data.js                     # База данных картин
├── index.html                  # Веб-сайт
├── style.css                   # Стили
├── script.js                   # Клиентская логика
├── sw.js                       # Service Worker
├── manifest.json               # PWA манифест
├── package.json                # Зависимости
├── .env.example                # Пример .env
├── .gitignore                  # Игнорируемые файлы
└── README.md                   # Этот файл
```

## 🔧 Технический стек

### Backend:
- **Node.js + Express** — веб-сервер
- **node-telegram-bot-api** — Telegram бот
- **SQLite3** — база данных
- **YooKassa** — платежная система
- **Axios, Dotenv** — вспомогательные библиотеки

### Frontend:
- **HTML5/CSS3/ES6+** — веб-интерфейс
- **PWA** — Service Worker + Manifest
- **Адаптивный дизайн** — mobile-first

## 🎯 Основные функции

### Telegram Бот:
- 🎨 Выбор 11 картин из галереи
- 💳 Создание заказов с YooKassa оплатой
- 📋 Просмотр истории заказов
- 🔔 Уведомления администратора
- 📱 Поддержка Telegram MiniApp
- 🔄 API для веб-сайта

### Веб-сайт:
- 🖼️ Галерея с 11 картинами
- 📱 Модальные окна просмотра
- 🎯 Кнопка "Оформить заказ" → Telegram
- 📊 SEO (Open Graph, Twitter Cards)
- 📲 PWA (установка на устройство)

### Платежи:
- **YooKassa**: live и тестовый режимы
- Поддержка: ЮMoney, Тинькофф, Сбербанк
- Вебхуки для подтверждения оплаты
- Токены защиты заказов

## 🛡️ Безопасность

### Важные правила:
1. **НИКОГДА не коммитьте `.env` файл** — он содержит секреты
2. **Всегда используйте GitHub Secrets** для хранения токенов
3. **Регулярно обновляйте зависимости** — `npm audit fix`
4. **Валидируйте webhook подписи** — защита от подделки

### Rate Limiting:
```javascript
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100, // 100 запросов с одного IP
  message: 'Слишком много запросов, попробуйте позже'
});

app.use('/api/', limiter);
```

## 🔄 Автоматизация

### GitHub Actions Workflow:
- **При push в main** — автоматический деплой
- **Каждые 5 минут** — проверка активности бота
- **Автоматический перезапуск** при падении
- **Логирование** всех действий

### Как это работает:
```
GitHub Push → Actions → Deploy to GitHub Pages → Keep bot alive
     ↓
  Проверка каждые 5 минут
     ↓
  Перезапуск при необходимости
```

## 📊 Мониторинг

### Встроенные API endpoints:
- `GET /api/bot-status` — статус бота
- `GET /api/stats` — статистика
- `GET /api/order/:id` — информация о заказе
- `GET /api/order/:id/status` — статус заказа
- `POST /api/order/create` — создание заказа
- `POST /api/order/:id/paid` — подтверждение оплаты
- `POST /webhook/payment` — вебхук YooKassa

### Внешние сервисы (бесплатно):
- **UptimeRobot** — мониторинг доступности
- **Google Analytics** — аналитика посещений
- **Sentry (бесплатный тариф)** — отслеживание ошибок

## 🚨 Устранение проблем

### Бот не запускается:
```bash
# Проверьте логи
tail -f bot.log

# Проверьте переменные окружения
echo $TELEGRAM_BOT_TOKEN

# Перезапустите вручную
node bot.js
```

### Ошибки в GitHub Actions:
1. Откройте **Actions → Deploy FlexyFrame Bot**
2. Нажмите на последний запуск
3. Проверьте логи шагов
4. Убедитесь, что все секреты настроены

### Проблемы с платежами:
```bash
# Проверьте YooKassa конфигурацию
echo $YOOKASSA_SHOP_ID
echo $YOOKASSA_SECRET_KEY

# Убедитесь, что ключи не пустые
# Используйте тестовые ключи для разработки
```

## 📈 Производительность

### Оптимизации:
- **Lazy loading** изображений
- **Service Worker** кэширование
- **Content-visibility** для рендеринга
- **GPU ускорение** для анимаций
- **CDN от GitHub Pages** для статики

### Лимиты GitHub Pages:
- **Трафик**: 100 GB/месяц
- **Хранилище**: 1 GB
- **Uptime**: 99.9%
- **SSL**: Автоматический

## 🔐 Безопасность данных

### Что хранится в базе данных:
- **orders** — заказы (id, user_id, painting_id, price, status, payment_id, token)
- **users** — пользователи (user_id, username, first_name, last_name)
- **sessions** — сессии (user_id, state, data)

### Что НЕ хранится:
- ❌ Пароли (Telegram не требует)
- ❌ Банковские карты (YooKassa обрабатывает)
- ❌ Персональные данные (только ID и имя)

## 🎯 Дальнейшие шаги

### После деплоя:
1. ✅ Проверьте сайт: `https://yourusername.github.io/flexyframe`
2. ✅ Запустите бота: `@flexyframe_bot`
3. ✅ Протестируйте заказ
4. ✅ Настройте мониторинг (UptimeRobot)

### Для продакшена:
1. 🔒 Используйте продакшен ключи YooKassa
2. 🔒 Настройте вебхуки YooKassa
3. 🔒 Добавьте CSP заголовки
4. 🔒 Включите HTTPS only

## 📞 Поддержка

### Контакты:
- **Telegram**: @flexyframe_bot
- **Email**: designstudioflexyframe@gmail.com
- **Сайт**: https://yourusername.github.io/flexyframe

### Документация:
- [YooKassa API](https://yookassa.ru/developers)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [GitHub Pages](https://docs.github.com/en/pages)

## 📄 Лицензия

MIT License — свободное использование с указанием авторства.

---

**Создано с ❤️ для FlexyFrame**